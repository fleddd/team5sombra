const Header = () => {
  return (
    <header style={styles.header}>
      <h1>My dream team №5</h1>
    </header>
  );
};

const styles = {
  header: {
    padding: "1em",
    color: "#2964fbff",
    textAlign: "center",
    fontWeight: "bold",
  },
};

export default Header;
